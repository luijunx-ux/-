import 'package:flutter_test/flutter_test.dart';
import 'package:tianrenlu/main.dart';

void main() {
  test('application root can be constructed', () {
    expect(const TianrenluApp(), isNotNull);
  });
}
