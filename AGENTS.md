# 天人律 Codex 开发规则

## 强制读取顺序

每次开发前依次阅读 `PROJECT_CONTEXT.md`、`REQUIREMENTS_BASELINE.md`、`CURRENT_SPRINT.md` 及任务引用的专题文档。若文档冲突，以冻结需求中的最新决定为准；仍无法裁决时停止实现并请用户确认。

## 不可擅自改变

- 产品不是算命、娱乐星座或医疗诊断应用。
- 首页以“五运六气”和“星辰节律”为同等级双核心入口。
- Forest Life（森林生命）与 Obsidian Life（黑曜生命/黑曜科技）双主题方向。
- 确定性规则由可测试引擎计算，LLM 只负责解释与建议。

## UI 与提交硬门禁

- 当前 Sprint 未获得用户明确回复“视觉审核通过”前，禁止执行 `git add`、`git commit`、`git push`。
- “功能审核通过”“测试通过”“可以运行”均不等同于“视觉审核通过”。
- UI 变更必须按 `docs/ui/UI_ACCEPTANCE_CRITERIA.md` 提供双主题截图和自检结果。
- 视觉未通过时继续在工作区迭代；不得用自动测试代替人工视觉审核。

## 工程规则

- Flutter + FastAPI + PostgreSQL；公共 API 使用 `/api/v1`。
- 模块化、数据驱动、规则与解释分离、UI 与业务分离。
- Agent 通过受控工具读取结构化结果，不直接访问数据库。
- 不硬编码知识规则，不把 LLM 输出当作历法或天文计算结果。
- 不提交密钥、真实用户数据、构建物、缓存或本地环境文件。
- 内容须温和、非宿命、非恐吓；不得给出诊断、治疗或绝对预测。

## 完成报告

每轮报告修改文件、实现内容、验证结果、已知问题和下一步。未经用户明确授权不得提交；涉及 UI 时还必须满足视觉审核硬门禁。
